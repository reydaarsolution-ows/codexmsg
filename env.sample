# Copy to .env and adjust values
# Next.js client env must start with NEXT_PUBLIC_

# Backend
PORT=4000
CORS_ORIGIN=http://localhost:3000
REDIS_URL=
NODE_ENV=development

# Frontend
NEXT_PUBLIC_SOCKET_URL=http://localhost:4000
NEXT_PUBLIC_SOCKET_PATH=/ws
